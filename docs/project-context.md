[2026-05-28] Проблема: Усиление `GetLoginUser` проверкой существования пользователя сломало websocket capacity-тесты с synthetic session users, которых test helper не создавал в `users`. → Решение: Тестовые helpers должны создавать пользователей в DB перед выдачей session cookie для не-default админов.
[2026-05-28] Проблема: Browser API тест с `/api/login` получил `Invalid CSRF token`, потому что test DB не привела `webPath` к `/`, и CSRF login exemption ожидал другой base path. → Решение: В browser API integration tests явно инициализировать settings и выставлять `webPath=/` перед созданием `APIHandler`.
[2026-05-28] Проблема: Smoke backend `go run .` на временной базе завершился из-за production core config с naive outbound, который недоступен без build tag `with_naive_outbound`. → Решение: Для UI smoke использовать e2e/test panel harness или конфигурацию без production core startup вместо прямого запуска production app.
[2026-05-29] Проблема: Inline Node smoke с `require()` и top-level `await` падает на Node 25 с `ERR_AMBIGUOUS_MODULE_SYNTAX`. → Решение: Оборачивать ad-hoc smoke scripts в async function или переносить сценарий в Playwright spec.
[2026-05-29] Проблема: Smoke selector `.v-card` для карточки администратора также матчился на `.v-card` внутри confirm modal. → Решение: Для страницы админов сужать smoke selector до пользовательских карточек `.v-card.rounded-xl`.
[2026-05-29] Проблема: Smoke init script принудительно возвращал `sui:ui:mode=classic` при каждой навигации и мешал проверить Nexus shell. → Решение: Задавать начальный режим только перед login, а дальше переключать classic/nexus через реальный UI mode control.
[2026-05-29] Проблема: В e2e sandbox `go run` пытался создать Go telemetry token в `%APPDATA%` и получал `Access is denied`; `GOTELEMETRY=off` сам по себе не меняет `go env GOTELEMETRY`. → Решение: В e2e harness направлять `APPDATA` внутрь workspace и выставлять `GOTELEMETRYDIR` для backend process.
[2026-05-29] Проблема: После intentional wrong-password проверки Notivue toast перекрывал confirm modal и перехватывал Playwright click по `Delete`. → Решение: В admin smoke отключать pointer events у контейнера notifications, чтобы проверка формы не зависела от toast overlay timing.
[2026-05-29] Проблема: Playwright `webServer` на Windows показал `ok` по admin smoke, но процесс завис на shutdown и команда завершилась outer timeout. → Решение: Для разового smoke запускать e2e backend/frontend вручную, запускать spec с `SUI_E2E_SKIP_WEB_SERVER=1`, затем явно останавливать процессы.
[2026-05-29] Проблема: Admin smoke выполняет classic+nexus create/delete и отдельную cookie-session проверку, поэтому на холодном e2e окружении упёрся в общий Playwright timeout `45s`. → Решение: Для этого spec задать локальный timeout `90s`.
[2026-05-29] Проблема: Ad-hoc Node smoke runner показал `1 passed`, но завис после cleanup из-за живых child streams/process handles. → Решение: При таких runners ставить timeout на `taskkill`, destroy stdout/stderr streams и завершать процесс явно после успешного smoke.
[2026-05-29] Проблема: Финальный `go test ./...` прошёл пакеты, но завершился setup failure из-за доступа к `%LOCALAPPDATA%\go-build` в sandbox. → Решение: В sandbox запускать финальный Go прогон с `APPDATA` и `GOCACHE` внутри workspace.
[2026-05-29] Проблема: `TMP` внутри `tests/baseline/phase6/tmp` оставил Go build workdirs под обычной директорией repo, и `go test ./...` начал воспринимать их как пакеты. → Решение: Для Go temp/cache использовать dot-директории workspace (`.gotmp`, `.gocache`, `.appdata`) и очищать прежний generated tmp.
[2026-05-29] Проблема: `go test -race ./...` в CI мог недетерминированно пропускать overflow в `TestIntegrationRealtimeWSMaxPerIPCapacity`, потому что capacity-тесты держали много idle websocket-соединений с heartbeat `1s/1s`, и под race ранние соединения успевали закрыться до проверки лимита. → Решение: Для websocket capacity-тестов использовать router с длинным `pingInterval`, чтобы reservations оставались активными до overflow-dial.
