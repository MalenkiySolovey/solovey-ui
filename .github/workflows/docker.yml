name: Docker Image CI

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Release tag"
        required: true
        default: "v2026.3.0"
        type: string
      preflight_only:
        description: "Build release images without publishing"
        required: true
        default: true
        type: boolean
  push:
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}
  RELEASE_TAG_INPUT: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}

jobs:
  build:
    name: build-${{ matrix.platform_name }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { platform_name: amd64, platform: linux/amd64, runner: ubuntu-24.04 }
          - { platform_name: arm64, platform: linux/arm64, runner: ubuntu-24.04-arm }
    runs-on: ${{ matrix.runner }}
    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          submodules: recursive

      - name: Validate release tag against source version
        run: |
          set -euo pipefail
          VERSION="${RELEASE_TAG_INPUT#v}"
          EXPECTED_VERSION="$(tr -d '\r\n' < config/identity/version)"
          [[ "$VERSION" =~ ^[0-9]+\.[0-9]+\.[0-9]+([.-][0-9A-Za-z.-]+)?$ ]]
          [ "$VERSION" = "$EXPECTED_VERSION" ] || { echo "release tag $RELEASE_TAG_INPUT does not match source version $EXPECTED_VERSION" >&2; exit 1; }

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      - name: Log in to GHCR
        if: github.event_name == 'push' || inputs.preflight_only == false
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Normalize release tag
        id: vars
        run: |
          TAG="${RELEASE_TAG_INPUT}"
          case "$TAG" in
            v*) echo "tag=$TAG" >> "$GITHUB_OUTPUT" ;;
            *) echo "tag=v$TAG" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Normalize image name
        id: image
        run: echo "name=${IMAGE_NAME,,}" >> "$GITHUB_OUTPUT"

      - name: Validate release trust root
        env:
          SUI_RELEASE_TRUST_ROOTS_B64: ${{ vars.SUI_RELEASE_TRUST_ROOTS_B64 }}
        run: |
          set -euo pipefail
          [ -n "$SUI_RELEASE_TRUST_ROOTS_B64" ] || { echo "SUI_RELEASE_TRUST_ROOTS_B64 repository variable is required" >&2; exit 1; }
          node --input-type=module - <<'NODE'
          const encoded = process.env.SUI_RELEASE_TRUST_ROOTS_B64;
          let roots;
          try { roots = JSON.parse(Buffer.from(encoded, 'base64').toString('utf8')); } catch { throw new Error('SUI_RELEASE_TRUST_ROOTS_B64 is not valid base64 JSON'); }
          if (!Array.isArray(roots) || roots.length === 0 || roots.length > 8) throw new Error('SUI_RELEASE_TRUST_ROOTS_B64 must contain one to eight roots');
          NODE

      - name: Preflight Docker image
        if: github.event_name == 'workflow_dispatch' && inputs.preflight_only == true
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platform }}
          push: false
          tags: ${{ steps.image.outputs.name }}:preflight-${{ matrix.platform_name }}
          build-args: |
            SUI_RELEASE_TRUST_ROOTS_B64=${{ vars.SUI_RELEASE_TRUST_ROOTS_B64 }}
          cache-from: type=gha,scope=docker-${{ matrix.platform_name }}
          cache-to: type=gha,mode=max,scope=docker-${{ matrix.platform_name }}

      - name: Build and push Docker image by digest
        if: github.event_name == 'push' || inputs.preflight_only == false
        id: build
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7
        with:
          context: .
          file: Dockerfile
          platforms: ${{ matrix.platform }}
          outputs: type=image,push-by-digest=true,name-canonical=true,push=true
          tags: ${{ steps.image.outputs.name }}
          build-args: |
            SUI_RELEASE_TRUST_ROOTS_B64=${{ vars.SUI_RELEASE_TRUST_ROOTS_B64 }}
          cache-from: type=gha,scope=docker-${{ matrix.platform_name }}
          cache-to: type=gha,mode=max,scope=docker-${{ matrix.platform_name }}

      - name: Export image digest
        if: github.event_name == 'push' || inputs.preflight_only == false
        run: |
          mkdir -p "${RUNNER_TEMP}/digests"
          DIGEST="${{ steps.build.outputs.digest }}"
          touch "${RUNNER_TEMP}/digests/${DIGEST#sha256:}"

      - name: Upload image digest
        if: github.event_name == 'push' || inputs.preflight_only == false
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: docker-digest-${{ matrix.platform_name }}
          path: ${{ runner.temp }}/digests/*
          if-no-files-found: error
          retention-days: 1

  publish:
    name: publish-manifest
    runs-on: ubuntu-24.04
    needs: build
    if: github.event_name == 'push' || inputs.preflight_only == false
    steps:
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4

      - name: Log in to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Normalize release tag
        id: vars
        run: |
          TAG="${RELEASE_TAG_INPUT}"
          case "$TAG" in
            v*) echo "tag=$TAG" >> "$GITHUB_OUTPUT" ;;
            *) echo "tag=v$TAG" >> "$GITHUB_OUTPUT" ;;
          esac

      - name: Normalize image name
        id: image
        run: echo "name=${IMAGE_NAME,,}" >> "$GITHUB_OUTPUT"

      - name: Download image digests
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          pattern: docker-digest-*
          path: ${{ runner.temp }}/digests
          merge-multiple: true

      - name: Create and push multi-platform manifest
        working-directory: ${{ runner.temp }}/digests
        run: |
          set -e
          DIGESTS=""
          for digest_file in *; do
            [ -f "$digest_file" ] || continue
            DIGESTS="${DIGESTS} ${{ steps.image.outputs.name }}@sha256:${digest_file}"
          done
          [ -n "$DIGESTS" ] || { echo "no image digests were downloaded" >&2; exit 1; }
          docker buildx imagetools create \
            -t "${{ steps.image.outputs.name }}:${{ steps.vars.outputs.tag }}" \
            -t "${{ steps.image.outputs.name }}:latest" \
            $DIGESTS
