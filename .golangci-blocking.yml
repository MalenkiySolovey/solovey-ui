run:
  timeout: 10m

linters:
  disable-all: true
  enable:
    - bodyclose
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - noctx

linters-settings:
  govet:
    enable:
      - nilness
